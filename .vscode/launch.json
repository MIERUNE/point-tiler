{
	"version": "0.2.0",
	"configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug executable 'main'",
            "program": "${workspaceFolder}/target/release/examples/pointcloud_transform",
            "cargo": {
                "args": ["build", "--release", "--package", "pcd-transformer", "--example", "pointcloud_transform"],
                // "args": ["build", "--package", "pcd-transformer", "--example", "pointcloud_transform"],
                "filter": {
                    "name": "pointcloud_transform",
                },
            },
            "cwd": "${workspaceFolder}",
        }
	]
}
