[package]
name = "pcd-parser"
version.workspace = true
edition = "2021"

[dependencies]
csv = "1.3.0"
las = { version = "0.9.1", features = ["laz-parallel"] }
pcd-core = { path = "../pcd-core" }
rayon = "1.10.0"

[dev-dependencies]
